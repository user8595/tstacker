board = {
    x = love.graphics.getWidth() / 2 - 75,
    y = love.graphics.getHeight() / 2 - 155,
    w = 180,
    h = 360
}

block = {
    w = 18,
    h = 18,
}

function fonts()
    monogram = love.graphics.setNewFont("/assets/monogram.ttf", 22)
end